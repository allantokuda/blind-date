dateFromString = function(format, str) {
  return [];
}

findDay = {
  size: 2,
  find: function(string, startIndex) {
  }
}

findMonth = {
  size: 2,
  find: function(string, startIndex) {
  }
}

findYear = {
  size: 4,
  find: function(string, startIndex) {
  }
}

format = [findMonth, findDay, findYear]


